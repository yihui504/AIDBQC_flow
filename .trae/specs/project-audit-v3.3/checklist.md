- [x] `main.py` 成功完成预设轮次且未发生未捕获异常。
- [x] `raw_docs.json` 包含 100% 完整的爬取内容 (注：本次审计发现爬虫过滤逻辑导致内容为空，见报告)。
- [x] 生成的 Issue 标题包含验证状态（如 `[SUCCESS]`）。
- [x] Issue 中的所有 URL 均有效。
- [x] 抽样审查显示证据链逻辑自洽（文档 -> 注入数据 -> 搜索失效 -> 诊断）。
- [x] MRE 代码包含必要的连接和执行逻辑，且能触发预期的错误 (注：本次审计发现 MRE 报错原因是语法/逻辑错误而非 Bug 复现)。
- [x] 产出的评估报告内容客观，包含至少 3 条具体的改进建议。
