\*- [x] Agent 1 提取的合约已包含操作时序（operational_sequences）和状态依赖要求
- [x] 每一个契约项均通过 `source_urls` 映射到了真实的文档 URL
- [x] Agent 2 能够生成触发非法时序（如先 Search 后 Create）的异常测试用例
- [x] Agent 6 生成的 Issue 引用了最初在合约中定义的 URL，且经过了真实性和相关性验证

